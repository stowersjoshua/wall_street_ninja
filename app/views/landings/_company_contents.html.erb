<% if @companies.present? %>
	<table class="table table-striped table-hover">
		<thead>
			<th>Index</th>
			<th>Company Name</th>
			<th>city</th>
			<th>State</th>
			<th>Action</th>
		</thead>
		<tbody>
			<% @companies.each_with_index do |company, index| %>
				<tr>
					<td><%= index+1 %>.</div>
					<td><%= company.name %></td>
					<td><%= company.city %></td>
					<td><%= company.state %></td>
					<td><%= link_to "Company Details", company_details_landing_path(company) %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
<% end %>