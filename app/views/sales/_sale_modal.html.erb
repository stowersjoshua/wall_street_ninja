<div id="saleModel" class="modal fade in custom_modal" tabindex="-1" role="dialog" style="display: block;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close close_btn">&times;</button>
        <h4 class="modal-title">Sell Stock</h4>
      </div>
      <%= simple_form_for(Sale.new, url: company_sales_path(@company)) do |f| %>
        <div class="modal-body">
            <div class="row">
              <div class="col-md-4 cmpny_label">
                Company Name
              </div>
              <div class="col-md-8">
                <div class="form-inputs">
                  <%= text_field_tag :company, @company.name, class: "form-control name_fld", readonly: true %>
                  <%= hidden_field_tag :purchase_id, @purchase.id %>
                </div>
              </div>
            </div>
            <div class="row">
              <div class="col-md-4 cmpny_label">
                Quantity
              </div>
              <div class="col-md-8">
                <div class="form-inputs">
                  <%= f.input :quantity, collection: (1..@purchase.quantity), label: false, prompt: "Select stock quantity" , input_html: {class: "form-control name_fld"} %>
                </div>
              </div>
            </div>
          </div>
          <div class="modal-footer">
            <%= f.submit "Sold", class: "btn btn-primary sold_btn" %>
            <button type="button" class="btn btn-default close_btn">Close</button>
          </div>
        <% end %> 
    </div>
  </div>
</div>